/**
 * OrganizationSwitcherSheet is no longer used after org removal.
 * Kept as a stub to avoid breaking imports.
 */
export function OrganizationSwitcherSheet(_props: Record<string, unknown>) {
	return null;
}
